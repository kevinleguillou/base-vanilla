{
  "name": "base-vanilla",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Kevin Le Guillou <contact@kevinleguillou.com>",
  "license": "MIT",
  "scripts": {
    "build": "node-sass --output-style=compressed scss --output=dist",
    "watch": "node-sass --watch --output-style=compressed scss --output=dist",
    "sass": "yarn run build && yarn run watch"
  },
  "dependencies": {
    "node-sass": "^4.14.1"
  }
}
